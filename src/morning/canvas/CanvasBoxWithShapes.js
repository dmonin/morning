/**
 * @fileoverview Canvas Box with Shape.
 */

goog.provide('morning.canvas.CanvasBoxWithShape');

/**
 * Canvas Box with Shape
 *
 * @constructor
 * @extends {goog.events.EventTarget}
 */
morning.canvas.CanvasBoxWithShape = function()
{
    goog.base(this);

};

goog.inherits(morning.canvas.CanvasBoxWithShape, goog.ui.Component);
